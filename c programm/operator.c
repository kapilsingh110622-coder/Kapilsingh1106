#include<stdio.h>
int main()
{
	 int a,b;
	 
	 printf("enter two  intager number");
	 scanf("%d %d",&a,&b);
	 printf("the sum of a+b=%d",(a+b));
	 printf("the sudtraction of (a-b=%d",(a-b));
	 printf("the multipliction of a*b=%d",(a*b));
	 printf("the division of a/b=%d",(a/b));
	 printf("the modulo division a%b=%d",(a%b));
	 return 0;
	 
	 
}
